spring:
   cloud:
     config:
       uri: http://192.168.38.128:8888
       name: redis-8502   #这个对应github的config-repo目录的application-dept-dev.yml文件的application-dept
       label: master
       profile: dev # application-dept-dev.yml文件后面的dev